# Implementation Convention

`/implementation` refers to the current implementation tracker for this repository.

## Canonical File

The source of truth is:

- [implementation.md](/Users/theivendrankabelan/Documents/python_scripts/incident_triaging_n_resolutions/implementation.md)

## How To Use It

When you want to update implementation status or change execution order, use `/implementation` in your request and describe the change.

Examples:

- `/implementation mark step 5 as in progress`
- `/implementation add a note that pytest is blocked until dependencies are installed`
- `/implementation reorder Phase 1 to build MCP before readiness checks`

## Editing Rule

- Update `implementation.md` directly.
- Keep it focused on execution status and next steps.
